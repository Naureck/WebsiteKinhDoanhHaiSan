@model List<Model.ViewModel.OrderDetailViewModel>
@using PagedList.Mvc;
@using Model;
@{
    ViewBag.Title = "OrderDetail";
    Layout = "~/Areas/Admin/Views/Shared/_Layouts.cshtml";
}

<div class="row">

    <div class="col-lg-12">

        <div class="panel panel-default">
            <div class="panel-heading">
                Danh sách đơn hàng
            </div>

            <!-- /.panel-heading -->
            <div class="panel-body">
                <!--
                {
                    <div class="row form-group">
                        <div class="col-lg-12">
                            <div class="col-md-4">
                                <input type="text" value="" class="form-control" name="searchString" />
                            </div>
                            <div class="col-md-4">
                                <button type="submit" class="btn btn-success">Tìm kiếm</button>-->
                @*<a class="btn btn-primary" href="/Admin/ProductCategory/Create">Thêm mới</a>*@
                <!--</div>
                        </div>
                    </div>
                }-->

                <div class="table-responsive">
                    <table class="table table-striped table-bordered table-hover">
                        <thead>
                            <tr>

                                <th>Mã sản phẩm</th>
                                <th>Tên sản phẩm</th>
                                <th>Ảnh</th>
                                <th>Mã đơn hàng</th>
                                <th>Số lượng sản phẩm</th>
                                <th>Giá sản phẩm</th>
                                <th>#</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                var link = "/chi-tiet-don-hang/" + item.OrderID;
                                <tr id="row_@item.OrderID">

                                    <td>@item.ProductID</td>
                                    <td>@item.OrderID</td>
                                    <td>@item.Quantity</td>
                                    <td>@item.Price</td>

                                    <td>
                                        @Ajax.ActionLink("Xoá", "Delete", new { @id = item.OrderID }, new AjaxOptions
                                   {
                                       Confirm = "Bạn có muốn xoá bản ghi này?",
                                       OnComplete = "$('#row_" + @item.OrderID + "').remove()",
                                       HttpMethod = "Delete"
                                   })
                                    </td>
                                </tr>
                            }


                        </tbody>
                    </table>
                    @*@Html.PagedListPager(Model, page => Url.Action("Index", new { page, @searchString = @ViewBag.OrderID }))*@
                </div>
                <!-- /.table-responsive -->
            </div>
            <!-- /.panel-body -->
        </div>
    </div>
</div>

